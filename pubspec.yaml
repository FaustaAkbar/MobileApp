name: myapp
description: "My Flutter project."

publish_to: 'none' 
version: 1.0.0+1

# -------------- flutter version ---------
#flutter version is 3.22 or above
environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.22.0'
# --------------- PACKAGES----------------
dependencies:
  flutter:
    sdk: flutter
  
  # Utility Packages
  http: ^1.3.0
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0

  # State manajemen
  get: ^4.6.6
# --------------- PACKAGES END -----------

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:

  uses-material-design: true

  # ------------ LOCAL ASSETS ------------
  assets:
    - assets/background/
    - assets/icon/